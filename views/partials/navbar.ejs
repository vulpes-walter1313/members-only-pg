<nav class="bg-slate-900 p-4">
  <div class="max-w-5xl mx-auto flex justify-between items-center">
    <p class="text-3xl font-bold text-slate-100"><a href="/">VIP Message Board</a></p>
    <div class="flex gap-6">
      <% if (locals.currentUser) { %>
        <div class="w-9 h-9 rounded-full bg-slate-600 text-slate-50 flex justify-center items-center"><%= locals.currentUser.first_name.slice(0,1).toUpperCase() %></div>
        <a href="/posts/create" class="bg-slate-100 hover:bg-slate-300 text-slate-900 px-6 py-2 rounded-lg block">Create Post</a>
        <a href="/logout" class="bg-slate-900 text-slate-100 hover:bg-slate-800 border-2 border-slate-100 px-6 py-2 rounded-lg block">Logout</a>
      <% } else { %>
        <a href="/login" class="bg-slate-100 hover:bg-slate-300 text-slate-900 px-6 py-2 rounded-lg block">Login</a>
        <a href="/signup" class="bg-slate-900 text-slate-100 border-2 hover:bg-slate-800 border-slate-100 px-6 py-2 rounded-lg block">Sign Up</a>
      <% } %>
    </div>
  </div>
</nav>
<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/navbar') %> 
  <main class="bg-slate-950 px-4 py-14 min-h-screen flex flex-col gap-10">
    <div>
      <h1><%= title %></h1>
    </div>
    <div class="flex flex-col gap-8 max-w-xl mx-auto">
      <!-- This is where the post content will go -->
      <div class="bg-slate-800 p-4 rounded-md flex flex-col gap-6">
        <div class="text-slate-50">
          <p class="font-medium"><%= post.author_name%></p>
          <p class="text-sm font-light text-slate-300">Posted on <%= post.created_at %></p>
          <p class="text-sm font-light text-slate-300">Updated on <%= post.updated_at %></p>
        </div>
        <div class="text-slate-100 flex flex-col gap-2">
          <h1 class="text-3xl font-bold"><%= post.title %></h1>
          <p class="whitespace-pre-line"><%= post.body %>...</p>
        </div>
      </div>
    </div>
      <form action="/posts/<%= post.id%>/delete" method="POST" class="flex gap-6 mx-auto">
        <input type="hidden" name="postId" value="<%= post.id %>"/>
        <button class="bg-red-700 text-red-50 text-lg font-medium px-6 py-2 block rounded-lg" type="submit">Delete Post</button> 
      </form>
  </main>
</body>
</html>